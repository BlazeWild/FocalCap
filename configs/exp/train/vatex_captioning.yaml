# @package _global_

defaults:
  - /train
  - /dataset@train_dataloader.dataset: vatex
  - /dataset@val_dataloader.dataset: vatex
  - /exp/train/base
  - _self_

trainer:
  default_root_dir: "./logs/sig6k_full"
